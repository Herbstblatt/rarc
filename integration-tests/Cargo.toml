[package]
name = "integration-tests"
version = "0.1.0"
edition = "2024"

[dev-dependencies]
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
tempfile = "3.27.0"
toml = "0.8"
wait-timeout = "0.2"
